import * as React from 'react';
import { Typography } from '@mui/material';
import CommonLayout from "@/components/layout/CommonLayout";

const SalesDailyPage = () => {
    return (
        <CommonLayout>
            <Typography variant="h4">Daily Sales Report</Typography>
            {/* Sales 관련 정보를 보여주는 추가 컴포넌트 또는 로직 */}
        </CommonLayout>
    );
};

export default SalesDailyPage;

